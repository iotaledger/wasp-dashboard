export enum Action {
    Delete = "delete",
    Edit = "edit",
    Add = "add",
}

export enum LocalStorageKey {
    Chains = "chains",
    PersistedDataVersion = "persisted-data-version",
    Theme = "theme",
    ShowHexAsText = "show-hex-as-text",
    DashboardAuthRequired = "dashboard-auth-required",
}
